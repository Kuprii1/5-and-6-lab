<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title>Розсилки</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container my-5">

<h1 class="mb-4">Розсилки</h1>

<a href="/messages/new" class="btn btn-primary mb-3">+ Нова розсилка</a>

<ul class="list-group">
    <% messages.forEach(msg => { %>
        <li class="list-group-item d-flex justify-content-between align-items-center">
            <div>
                <strong><%= msg.subject %></strong><br>
                <small><%= msg.sentAt?.toLocaleDateString() %></small>
            </div>
            <div>
                <a href="/messages/<%= msg._id %>" class="btn btn-sm btn-info me-2">Переглянути</a>
                <a href="/messages/<%= msg._id %>/edit" class="btn btn-sm btn-warning">Редагувати</a>
            </div>
        </li>
    <% }) %>
</ul>

</body>
</html>
