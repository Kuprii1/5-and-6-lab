<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title><%= message.subject %> — Повідомлення</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container my-5">

<h1 class="mb-4">Перегляд розсилки</h1>

<div class="card mb-4">
    <div class="card-header">
        <h5 class="mb-0">Тема: <%= message.subject %></h5>
    </div>
    <div class="card-body">
        <p class="card-text"><strong>Текст:</strong></p>
        <p><%= message.content %></p>

        <p class="mt-4 mb-0 text-muted">
            <strong>Надіслано:</strong>
            <%= message.sentAt ? message.sentAt.toLocaleString('uk-UA') : 'невідомо' %>
        </p>
    </div>
</div>

<a href="/messages" class="btn btn-secondary">Назад</a>

</body>
</html>
