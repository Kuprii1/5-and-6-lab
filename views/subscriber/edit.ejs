<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title>Редагувати передплатника</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container my-5">

<h1 class="mb-4">Редагувати передплатника</h1>

<form action="/subscribers/<%= subscriber._id %>/update" method="POST" class="border p-4 rounded shadow-sm bg-light">
    <div class="mb-3">
        <label class="form-label">Ім’я:</label>
        <input type="text" name="name" value="<%= subscriber.name %>" class="form-control" required>
    </div>

    <div class="mb-3">
        <label class="form-label">Email:</label>
        <input type="email" name="email" value="<%= subscriber.email %>" class="form-control" required>
    </div>

    <div class="mb-3">
        <label class="form-label">Обліковий запис:</label>
        <input type="text" name="account" value="<%= subscriber.account %>" class="form-control" required>
    </div>

    <div class="mb-3">
        <label class="form-label">Пароль:</label>
        <input type="password" name="password" value="<%= subscriber.password %>" class="form-control" required>
    </div>

    <button type="submit" class="btn btn-success">Оновити</button>
    <a href="/subscribers" class="btn btn-secondary ms-2">Скасувати</a>
</form>

</body>
</html>
