<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title>Список передплатників</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container my-5">

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="mb-0">Список передплатників</h1>
    <a href="/subscribers/new" class="btn btn-primary">Новий передплатник</a>
</div>

<% if (subscribers.length === 0) { %>
    <div class="alert alert-warning">Немає передплатників.</div>
<% } else { %>
    <ul class="list-group">
        <% subscribers.forEach(sub => { %>
            <li class="list-group-item d-flex justify-content-between align-items-center">
                <div>
                    <strong><%= sub.name %></strong>
                    <span class="text-muted">&lt;<%= sub.email %>&gt;</span>
                </div>
                <div class="d-flex gap-2">
                    <a href="/subscribers/<%= sub._id %>/edit" class="btn btn-sm btn-outline-primary">Редагувати</a>
                    <form action="/subscribers/<%= sub._id %>/delete" method="POST">
                        <button type="submit" class="btn btn-sm btn-outline-danger">Видалити</button>
                    </form>
                </div>
            </li>
        <% }) %>
    </ul>
<% } %>

<a href="/" class="btn btn-secondary mt-4">На головну</a>

</body>
</html>
